## commands to run 
- pip install -r requirements.txt
- python main.py

## To Deploy the Application 
- Deploy (ONLY if billing enabled)
<b>gcloud app deploy<b>
- Open deployed app
 <b>gcloud app browse<b>